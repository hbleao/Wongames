import type { NextPage } from 'next';

import { Button } from 'components/Button';

const Home: NextPage = () => {
  return (
    <div>
      <Button />
      <p>testing...</p>
    </div>
  );
};

export default Home;
